/* import { Checkbox } from "@mui/material";
import { useEffect, useState } from "react";

export const CustomizedCheckBox = (initialChecked) => {
  const [itemChecked, setItemChecked] = useState(initialChecked);

  useEffect(() => {
    setItemChecked()
  }, [])
  
  return <Checkbox checked={itemChecked} />;
};
 */
